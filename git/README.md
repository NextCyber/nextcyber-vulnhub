# Git 服务

Git 是分布式版本控制系统，支持 ssh、git、https 三种协议来传递项目。

## 漏洞列表

| CVE 编号 | 漏洞类型 | 危险等级 |
|----------|----------|----------|
| CVE-2017-8386 | GIT-SHELL 沙盒绕过 | 🔴 高危 |

## 概述

GIT-SHELL 是 git 服务中重要的组成部分，允许用户通过 ssh 访问 git 仓库时执行受限的命令集。CVE-2017-8386 漏洞允许攻击者绕过 git-shell 的沙盒限制，读取任意文件甚至执行任意命令。
